[package]
name = "gorm/annotations"
version = "0.1.0"
description = "GORM custom annotations - synth declarations that lower @PrimaryKey / @Column / @Unique / @Index / @Timestamps / @SoftDelete etc. to the @structTag(\"gorm\", ...) form GORM reflects on at runtime."
license = "MIT"

[dependencies]
